import { ViewStyle } from 'react-native';

const container: ViewStyle = {
  flexDirection: 'row',
  gap: 5,
  justifyContent: 'space-evenly',
  marginTop: 8,
};

export const styles = {
  container,
};
